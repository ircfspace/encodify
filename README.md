# Encodify | مخدوش‌گر (نسخه ۲ - پیشرفته)

<div dir="rtl">

**ابزار امن‌سازی و مخدوش‌سازی هوشمند جهت عبور از فیلترینگ و نظارت محتوا**

این پروژه یک ابزار تحت وب (Web Application) کاملاً کلاینت‌ساید است که به شما امکان می‌دهد متن‌ها، لینک‌ها، کانفیگ‌ها و فایل‌های خود را به شکلی امن و غیرقابل شناسایی منتقل کنید. انکودیفای داده‌های شما را به **کلمات فارسی معنادار** (برای متن) یا **تصاویر PNG معمولی** (برای فایل) تبدیل می‌کند تا در بسترهای محدود یا شنود شده، قابل تشخیص نباشند.

---

## ✨ ویژگی‌های کلیدی

- **🔒 امنیت بالا:** استفاده از رمزنگاری پیشرفته **AES-GCM** (در صورت تنظیم کلید).
- **📝 بهینه‌سازی متن:** تبدیل متن‌ها به کلمات کوتاه ۲ و ۳ حرفی فارسی برای کاهش چشمگیر طول پیام.
- **🖼️ استگانوگرافی فایل (جدید):** تبدیل فایل‌ها به تصاویر `.png`. در این روش فایل شما درون پیکسل‌های تصویر مخفی می‌شود.
- **📉 حجم بهینه:** برخلاف نسخه‌های قبل، در این روش حجم فایل خروجی تقریباً برابر با فایل اصلی (**۱ تا ۱.۵ برابر**) است و برای فایل‌های حجیم نیز مناسب است.
- **📲 نسخه وب اپلیکیشن (PWA):** قابلیت نصب روی گوشی و کامپیوتر و استفاده **کاملاً آفلاین** (بدون نیاز به اینترنت).
- **⚡ سرعت بالا:** فشرده‌سازی خودکار داده‌ها (Gzip) قبل از رمزنگاری.
- **🛡️ حریم خصوصی:** تمام عملیات در مرورگر شما انجام می‌شود و هیچ داده‌ای به سرور ارسال نمی‌گردد.

---

## 🚀 نحوه نصب و استفاده آفلاین

این ابزار یک **PWA** است. برای نصب:
1. سایت را در مرورگر (Chrome یا Safari) باز کنید.
2. روی دکمه **"نصب نسخه آفلاین"** در بالای صفحه کلیک کنید (یا از منوی مرورگر گزینه *Add to Home Screen* را بزنید).
3. برنامه مانند یک اپلیکیشن بومی روی دستگاه شما نصب شده و بدون اینترنت کار می‌کند.

---

## 📖 راهنمای استفاده

### 🅰️ بخش متن (Text Operations)

مناسب برای ارسال کانفیگ V2Ray، لینک‌ها یا پیام‌های متنی محرمانه.

**برای رمزنگاری:**
1. متن خود را در کادر ورودی بنویسید.
2. (اختیاری) یک **رمز عبور** تعیین کنید.
3. دکمه **🔒 رمزنگاری متن** را بزنید.
4. خروجی (کلمات فارسی) را کپی کنید.

**برای رمزگشایی:**
1. متن مخدوش (کلمات فارسی) را در ورودی قرار دهید.
2. اگر رمز دارد، آن را وارد کنید.
3. دکمه **🔓 رمزگشایی متن** را بزنید تا متن اصلی نمایان شود.

---

### 📂 بخش فایل (File Operations)

مناسب برای ارسال فایل‌های فشرده، اسناد، APK و... در قالب عکس.

**برای رمزنگاری (تبدیل فایل به عکس):**
1. فایل خود را انتخاب کنید یا در کادر بکشید و رها کنید.
2. (اختیاری) رمز عبور بگذارید.
3. دکمه **⬇️ رمزنگاری و دانلود فایل** را بزنید.
4. خروجی یک تصویر با پسوند `.E.png` خواهد بود که فایل شما در آن مخفی شده است.

**برای رمزگشایی (استخراج فایل از عکس):**
1. تصویر رمزنگاری شده (`.E.png`) را انتخاب کنید.
2. رمز عبور را وارد کنید.
3. دکمه **⬇️ رمزگشایی و دانلود فایل** را بزنید.
4. فایل اصلی شما با پیشوند `_D` دانلود می‌شود.

---

## ⚠️ نکات فنی و امنیتی

- **فرمت فایل‌ها:** فایل‌های خروجی بخش فایل، تصاویر معتبر `PNG` هستند و در گالری‌ها قابل مشاهده‌اند (به صورت نویز رنگی)، اما ماهیت اصلی فایل برای سیستم‌های فیلترینگ مخفی می‌ماند.
- **حجم فایل:** به دلیل استفاده از تکنیک نگاشت پیکسل، افزایش حجم بسیار ناچیز است (حدود ۱ تا ۱.۵ برابر).
- **کلید رمز:** اگر برای داده‌های خود رمز بگذارید، بدون آن رمز به هیچ عنوان (حتی توسط سازنده) قابل بازگشایی نیستند.

---

**توسعه داده شده برای آزادی جریان اطلاعات** تقدیم به مردم شریف ایران ❤️

*تهیه شده توسط IRCF*
*توسعه داده شده توسط DanTch*

</div>